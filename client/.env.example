# Base URL API server.
# Kontrak (lihat src/lib/api.ts): VITE_API_URL ?? http://localhost:9000/api/v1
# - Development lokal: TIDAK PERLU file .env — fallback otomatis ke localhost.
# - Production: set Environment Variable di Vercel, nilai HARUS menyertakan suffix /api/v1.
VITE_API_URL="URL API"
